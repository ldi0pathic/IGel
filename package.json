{
  "name": "igel",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24.0.0"
  },
  "scripts": {
    "build": "node build.js",
    "build:zip": "node build.js --zip",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "fflate": "^0.8.2",
    "jsdom": "^30.1.0",
    "vitest": "^4.1.11"
  }
}
